def image_to_video(image_url: str):
    # Placeholder: later we will add real AI conversion
    fake_output = f"https://example.com/image_to_video/{image_url.split('/')[-1]}.mp4"
    return fake_output
